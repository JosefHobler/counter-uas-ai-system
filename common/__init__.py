"""Shared code used by both the edge node and the ground station.

Kept as a small monorepo-style package so the telemetry contract and the
telemetry client have a single source of truth instead of being copy-pasted
across the two deployable nodes.
"""
